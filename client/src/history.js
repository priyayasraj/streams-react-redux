import createHistory from 'history/createBrowserHistory'; //creating history object so that we can easily change on which page the currently 
                                                            //user is.Its already inside react router dom no need install
export default createHistory();